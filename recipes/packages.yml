---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    repos:
      copr:
        - atim/resources
    install:
      packages:
        - micro
        - qt5ct
        - qt6ct
        - fuse-libs
        - mangohud
        - resources
    remove:
      packages:
        - firefox
        - firefox-langpacks
